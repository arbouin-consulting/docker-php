Docker Hub for ARBOUIN Consulting
=================================

Docker's images used for project development.

Default install :
 * wkhtmltopdf
 * zsh
 * oh-my-zsh
 * composer


Command for local build:
```shell
$ cd phpX.Y-fpm && docker build -t phpX.Y-fpm-test ./
```